# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy your static HTML files to the Nginx container
COPY . /usr/share/nginx/html

# Expose port 80 to allow access to the website
EXPOSE 80
